[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agent-runtime-kit"
version = "0.1.0"
description = "A framework-neutral runtime toolkit for observable AI agents with PostgreSQL-backed memory."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Shengtao Lin" }]
dependencies = [
  "pydantic>=2.9,<3",
  "pydantic-settings>=2.5,<3",
]

[project.optional-dependencies]
postgres = ["alembic>=1.13,<2", "psycopg[binary]>=3.2,<4", "sqlalchemy[asyncio]>=2.0,<3"]
service = ["fastapi>=0.115,<1", "uvicorn[standard]>=0.30,<1"]
telemetry = ["opentelemetry-api>=1.27,<2", "opentelemetry-sdk>=1.27,<2"]
adapters = ["langchain-core>=0.3,<2", "langgraph>=0.2,<2"]
dev = [
  "pyright>=1.1.380",
  "pytest>=8.3,<9",
  "pytest-asyncio>=0.24,<1",
  "ruff>=0.7,<1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/agent_runtime"]

[tool.pytest.ini_options]
addopts = "-ra"
asyncio_mode = "auto"
testpaths = ["tests"]
markers = ["integration: requires a real PostgreSQL database"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC", "RUF"]

[tool.pyright]
include = ["src", "tests"]
pythonVersion = "3.11"
typeCheckingMode = "strict"
venvPath = "."
venv = ".venv"
